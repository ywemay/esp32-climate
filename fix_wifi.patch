--- esp32_climate.ino	2026-04-15 10:35:00.000000000 +0800
+++ esp32_climate_fixed.ino	2026-04-15 11:20:00.000000000 +0800
@@ -1,5 +1,6 @@
 /*
   ESP32-C3 Climate Sensor with Web Interface
   Starts in AP mode for WiFi configuration
   Features WiFi network scanning for easy setup
+  Version 1.2 - Fixed WiFi connection issues
 */
 
@@ -27,6 +28,11 @@
 unsigned long lastReadingTime = 0;
 bool wifiConnected = false;
 bool apMode = false;
+
+// WiFi reconnection state
+unsigned long lastConnectionAttempt = 0;
+int connectionRetryCount = 0;
+const unsigned long MIN_RECONNECT_INTERVAL = 30000; // 30 seconds between reconnection attempts
+const int MAX_CONNECTION_RETRIES = 5; // After 5 failures, fall back to AP mode
 
 void saveConfig() {
   preferences.begin(NVS_NAMESPACE, false);
@@ -52,25 +58,56 @@
 }
 
 void connectToWiFi() {
   if (wifiSSID.length() == 0) {
     wifiConnected = false;
     return;
   }
   
-  Serial.print("Connecting to WiFi: ");
+  // Check if we should attempt reconnection
+  unsigned long now = millis();
+  if (connectionRetryCount > 0 && (now - lastConnectionAttempt) < MIN_RECONNECT_INTERVAL) {
+    return; // Too soon to retry
+  }
+  
+  // After too many failures, fall back to AP mode
+  if (connectionRetryCount >= MAX_CONNECTION_RETRIES) {
+    Serial.println("Too many connection failures, falling back to AP mode");
+    if (!apMode) {
+      startAPMode();
+    }
+    return;
+  }
+  
+  connectionRetryCount++;
+  lastConnectionAttempt = now;
+  
+  Serial.print("Connecting to WiFi (attempt ");
+  Serial.print(connectionRetryCount);
+  Serial.print("/");
+  Serial.print(MAX_CONNECTION_RETRIES);
+  Serial.print("): ");
   Serial.println(wifiSSID);
+  
+  // Disconnect first to ensure clean state
+  WiFi.disconnect(true);
+  delay(100);
   
   WiFi.begin(wifiSSID.c_str(), wifiPassword.c_str());
   
-  for (int i = 0; i < 30; i++) {
+  // Try for up to 30 seconds (60 * 500ms)
+  for (int i = 0; i < 60; i++) {
     if (WiFi.status() == WL_CONNECTED) {
       wifiConnected = true;
+      connectionRetryCount = 0; // Reset on success
       Serial.print("Connected! IP: ");
       Serial.println(WiFi.localIP());
       return;
     }
     delay(500);
-    Serial.print(".");
+    if (i % 10 == 0) Serial.print(".");
   }
   
   wifiConnected = false;
   Serial.println("\nFailed to connect");
+  
+  // If this was the first attempt and it failed immediately, 
+  // check if credentials might be wrong or network unavailable
+  if (connectionRetryCount == 1) {
+    Serial.println("First connection attempt failed. Possible issues:");
+    Serial.println("1. WiFi credentials incorrect");
+    Serial.println("2. Network not in range");
+    Serial.println("3. Router issues");
+  }
 }
 
@@ -354,21 +391,39 @@
 void setup() {
   Serial.begin(115200);
   delay(1000);
   
   Serial.println("\nESP32-C3 Climate Sensor");
   Serial.println("========================");
-  Serial.println("Version 1.1 - With WiFi Scanning");
+  Serial.println("Version 1.2 - Fixed WiFi Connection");
+  Serial.println("Changed config button from GPIO 2 to GPIO 4");
   
-  pinMode(CONFIG_BUTTON_PIN, INPUT_PULLDOWN);
+  // Initialize button with pull-down and debounce
+  pinMode(CONFIG_BUTTON_PIN, INPUT_PULLDOWN);
+  
+  // Wait for GPIO to stabilize (important for ESP32-C3)
+  delay(100);
+  
+  // Simple button debounce - read multiple times
+  bool gpioTrigger = false;
+  int highReadings = 0;
+  for (int i = 0; i < 5; i++) {
+    if (digitalRead(CONFIG_BUTTON_PIN) == HIGH) {
+      highReadings++;
+    }
+    delay(10);
+  }
+  gpioTrigger = (highReadings >= 3); // Button is HIGH if 3 out of 5 readings are HIGH
+  
   dht.begin();
   loadConfig();
   
-  // Check if we should start in AP mode
-  bool gpioTrigger = (digitalRead(CONFIG_BUTTON_PIN) == HIGH);
   bool hasCredentials = (wifiSSID.length() > 0);
   
-  if (!hasCredentials || gpioTrigger) {
+  if (!hasCredentials) {
+    Serial.println("No WiFi credentials found, starting in AP mode");
+    startAPMode();
+  } else if (gpioTrigger) {
+    Serial.println("Config button pressed, starting in AP mode");
     Serial.println("Starting in AP mode");
     startAPMode();
   } else {
@@ -395,11 +450,11 @@
   server.handleClient();
   
   // Handle WiFi reconnection
   if (!apMode && WiFi.status() != WL_CONNECTED) {
     wifiConnected = false;
-    connectToWiFi();
+    // connectToWiFi() is now called with rate limiting from within the function
   }
   
   readSensor();
   delay(100);
 }